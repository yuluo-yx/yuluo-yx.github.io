const n=`---
slug: go GMP 并发模型
title: go GMP
date: 2026-07-25 15:37:49
authors: yuluo
tags: [Go]
keywords: [Go]
image: /img/golang/go-gmp.jpeg
---

Go 是一门天生支持并发的语言。

<!-- truncate -->

## 为什么不使用操作系统的线程模型，而是自己写了一套？

根本原因：太重。

操作系统线程的内存占用通常在 2~8 MB，而 Goroutine 初始仅需 2 KB。

于是，Go 决定自己编写一套基于用户态的轻量级线程模型。在最早期 Go 1.0 版本的时候，调度器没有 P 的角色在，只有 G（Goroutine） 和 M （Machine，代表物理线程）。

## 早期存在的问题

因为没有 P 的存在，所有的 M 都去一个全局协程队列里抢任务 G ld执行。这种设计非常简单，但很快在多核 CPU 下暴露出灾难性的后果---性能瓶颈。每一个 M 都要去全局队列里拿锁，成百上千个线程同时抢一把锁，导致严重的锁竞争。极其浪费资源。多核 CPU 的算力白白浪费在无意义的等锁上。

## GMP 模型的出现

为了解决此问题，Go 团队的 Dmitry Vyukov 提出了跨时代的 GMP 模型。

G（Goroutine）：任务代码，包含了代码指针，栈空间，调度状态，本身不具备执行能力；

M（Machine）：系统线程，操作系统内核管理的物理工作者，G 最终绑定到 M 上，由 M 驱动 CPU 来执行；

P（Processor）：逻辑处理器，代表着执行 G 所需要的资源和上下文。

在此架构中，M 并不直接接触 G，而是通过 P 来间接获取 G

### 分析：为什么加入了 P 就能解决锁竞争？

因为 P 引入了本地队列，每个 P 都有一个容量为 256 的本地队列。但使用 go fun() 创建了一个新协程时，会优先放入当前 P 的本地队列中去，每个 M 必须绑定一个 P，然后只从绑定的 P 中获取 G 来执行。此时，因为本地队列只能由当前绑定的 M 访问，所以不需要加锁。从而避免了锁竞争的问题。

> 早期的全局队列，现在只作为一个备份存在。只当本地队列装不下时（溢出）或者完全没有任务时才会启用。

## 调度问题：如果多核 CPU 时，有的物理线程 M 任务完成空闲时，而有的 M 负载过重时，怎么处理？

工作窃取 Work  Stealing 正为了解决这一问题而生。

当某个 M 绑定的 P 自己的本地队列全部执行完成时，且全局队列没有 G 时，P 会将另一个（随机选择） P 的一半的 G 偷过来执行。

## 思考一个问题：当 G 发起了一个阻塞的系统调用时（比如读取大文件，发起网络请求），M 会被卡住，怎么办？

如果卡住时，后面的 G 一直等待，这是不可能的。此时，Go 调度器会立刻 启动 Hand Off 剥离机制。

调度器通过监控线程 sysmon，一旦发现 M 因为系统调度陷入阻塞，会立刻将 M 和当前的 P 强行解绑，寻找一个空闲的 M 或者新建一个 M 来处理 P 里面的后续的 G。至于卡住的 M，则继续等待 G 在内核里完成。

## 如果一个 G 写了死循环 for{}，且里面没有任何系统调用或函数跳转，会永远霸占 M 吗？

Go 1.14 之前，确实是这样的。当时的调度是协作式调度。只有在函数调用，栈扩容，G 才会主动让出 CPU。

在 1.14，引入了基于信号的抢占式调度：监控线程 sysmon 发现某个 G 连续运行超过 10 毫秒，就会向该 G 绑定的运行的 M 发送一个 SIGURG 信号。让 Go 彻底告别了恶意死循环卡死整个通道的旧时代。

## 如果此时没有需要执行的 G，即 P 里没有 G 了，M 怎么办？

M 空闲一段时间后，会进入自旋 - 再休眠 - 再回收，形成三层节流机制。

反过来亦是如此，G 有了，但是没有 M 时，也是先寻找自旋的 M - 没有，寻找休眠的 M - 继而再重新创建 M。

## 串联一个 Goroutine 的生命周期

1. 创建 — go 编译为 newproc，分配 g 结构体，状态置 _Grunnable，优先放入当前 P 的本地队列（无锁）。
2. 溢出 — 本地队列满（256），半数 G 连同新 G 转移全局队列（加锁兜底）。
3. 获取执行权 — M 绑定 P，从本地队列取 G，状态转 _Grunning，跳入函数体。
4. 执行中 — 函数调用时协作式让出；死循环由 sysmon 超 10 ms 发 SIGURG 强制抢占。
5. 阻塞系统调用 — G 进 _Gsyscall，sysmon 强解绑 M-P，P 交空闲/新建 M 续跑后续 G，原 M 留待内核返回。
6. 负载不均 — 本地空 + 全局空时，随机偷另一 P 的一半 G（Work Stealing）。
7. 空闲节流 — 无 G 时 M 依次 自旋 → 休眠 → 回收；新 G 到来时反向 自旋 M → 唤醒 M → 新建 M。
8. 结束 — return 进 goexit，状态 _Gdead，栈清理，g 归还 P 的 gFree 池复用，M 取下一个 G。

![Go GMP](/img/golang/go-gmp.jpeg)
`;export{n as default};
